#include <iostream>

enum enMaxSize { MAXSIZE = 100 };

void insertElemToArray(int elem, int *arr, int &size)
{
    arr[size] = elem;
    size++;
}



int main(void)
{
    int arr[MAXSIZE];
    int size = 0;


    return (0);
}
